# Secrets — never commit real keys
*.key
*.pem
.env
secrets/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Podman runtime-generated secrets / rendered config
config/headplane/cookie-secret
config/headplane/api-key
config/headscale/preauth-key
config/headscale/config.runtime.yaml
